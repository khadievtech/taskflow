# TaskFlow Frontend

Будет собран на следующем шаге Phase 0: React + Vite + TypeScript.

Пока сознательно не трогаем — сначала закрепляем backend-скелет,
git-workflow и структуру монорепо, чтобы не распылять внимание на
несколько технологий одновременно.
