# Скопируйте в .env (без .example) рядом с docker-compose.yml.
# Docker Compose автоматически подхватывает .env из той же папки.
POSTGRES_USER=taskflow
POSTGRES_PASSWORD=taskflow
POSTGRES_DB=taskflow
